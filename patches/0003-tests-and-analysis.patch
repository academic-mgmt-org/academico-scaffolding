diff --git a/tests/Feature/Auth/AuthenticationTest.php b/tests/Feature/Auth/AuthenticationTest.php
index 371651a..0d11c14 100644
--- a/tests/Feature/Auth/AuthenticationTest.php
+++ b/tests/Feature/Auth/AuthenticationTest.php
@@ -2,9 +2,12 @@
 
 namespace Tests\Feature\Auth;
 
+use App\Contracts\GatewayClient;
+use App\Exceptions\GatewayRpcException;
 use App\Models\User;
 use Illuminate\Foundation\Testing\RefreshDatabase;
 use Laravel\Fortify\Features;
+use Mockery\MockInterface;
 use Tests\TestCase;
 
 class AuthenticationTest extends TestCase
@@ -20,10 +23,15 @@ class AuthenticationTest extends TestCase
 
     public function test_users_can_authenticate_using_the_login_screen(): void
     {
-        $user = User::factory()->create();
+        $this->mock(GatewayClient::class, function (MockInterface $mock): void {
+            $mock->shouldReceive('login')
+                ->once()
+                ->with('person@example.com', 'password')
+                ->andReturn($this->gatewaySession());
+        });
 
         $response = $this->post(route('login.store'), [
-            'email' => $user->email,
+            'email' => 'person@example.com',
             'password' => 'password',
         ]);
 
@@ -32,14 +40,28 @@ class AuthenticationTest extends TestCase
             ->assertRedirect(route('dashboard', absolute: false));
 
         $this->assertAuthenticated();
+        $response
+            ->assertSessionHas('gateway.access_token', 'access-token')
+            ->assertSessionHas('gateway.refresh_token', 'refresh-token')
+            ->assertSessionHas('gateway.session_id', 'session-id')
+            ->assertSessionHas('gateway.user_id', 'user-id');
+        $this->assertDatabaseHas('users', ['email' => 'person@example.com']);
     }
 
     public function test_users_can_not_authenticate_with_invalid_password(): void
     {
-        $user = User::factory()->create();
+        $this->mock(GatewayClient::class, function (MockInterface $mock): void {
+            $mock->shouldReceive('login')
+                ->once()
+                ->andThrow(new GatewayRpcException(
+                    'auth.v1.AuthService/Login',
+                    16,
+                    'Invalid credentials.',
+                ));
+        });
 
         $response = $this->post(route('login.store'), [
-            'email' => $user->email,
+            'email' => 'person@example.com',
             'password' => 'wrong-password',
         ]);
 
@@ -71,11 +93,50 @@ class AuthenticationTest extends TestCase
     public function test_users_can_logout(): void
     {
         $user = User::factory()->create();
-
-        $response = $this->actingAs($user)->post(route('logout'));
+        $this->mock(GatewayClient::class, function (MockInterface $mock): void {
+            $mock->shouldReceive('logout')
+                ->once()
+                ->with('access-token', 'refresh-token');
+        });
+
+        $response = $this->actingAs($user)
+            ->withSession([
+                'gateway.access_token' => 'access-token',
+                'gateway.refresh_token' => 'refresh-token',
+                'gateway.session_id' => 'session-id',
+            ])
+            ->post(route('logout'));
 
         $response->assertRedirect(route('home'));
 
         $this->assertGuest();
+        $response
+            ->assertSessionMissing('gateway.access_token')
+            ->assertSessionMissing('gateway.refresh_token')
+            ->assertSessionMissing('gateway.session_id');
+    }
+
+    /**
+     * @return array{
+     *     access_token: string,
+     *     refresh_token: string,
+     *     session_id: string,
+     *     user_id: string,
+     *     identifier: string,
+     *     email: string,
+     *     role: string
+     * }
+     */
+    private function gatewaySession(): array
+    {
+        return [
+            'access_token' => 'access-token',
+            'refresh_token' => 'refresh-token',
+            'session_id' => 'session-id',
+            'user_id' => 'user-id',
+            'identifier' => 'identifier',
+            'email' => 'person@example.com',
+            'role' => 'student',
+        ];
     }
 }
diff --git a/tests/Feature/DashboardTest.php b/tests/Feature/DashboardTest.php
index 3c5cd7d..fa16536 100644
--- a/tests/Feature/DashboardTest.php
+++ b/tests/Feature/DashboardTest.php
@@ -2,8 +2,10 @@
 
 namespace Tests\Feature;
 
+use App\Contracts\GatewayClient;
 use App\Models\User;
 use Illuminate\Foundation\Testing\RefreshDatabase;
+use Mockery\MockInterface;
 use Tests\TestCase;
 
 class DashboardTest extends TestCase
@@ -19,9 +21,50 @@ class DashboardTest extends TestCase
     public function test_authenticated_users_can_visit_the_dashboard(): void
     {
         $user = User::factory()->create();
-        $this->actingAs($user);
+        $this->mock(GatewayClient::class, function (MockInterface $mock): void {
+            $mock->shouldReceive('countUnread')
+                ->once()
+                ->with('access-token', 'user-id')
+                ->andReturn(1);
+            $mock->shouldReceive('unreadNotifications')
+                ->once()
+                ->with('access-token', 'user-id', 1)
+                ->andReturn([$this->notification('Unread from gateway')]);
+            $mock->shouldReceive('recentNotifications')
+                ->once()
+                ->with('access-token', 'user-id', 5)
+                ->andReturn([]);
+        });
+
+        $this->actingAs($user)->withSession([
+            'gateway.access_token' => 'access-token',
+            'gateway.user_id' => 'user-id',
+        ]);
 
         $response = $this->get(route('dashboard'));
-        $response->assertOk();
+        $response
+            ->assertOk()
+            ->assertSee('Unread from gateway');
+    }
+
+    /**
+     * @return array<string, bool|string>
+     */
+    private function notification(string $title): array
+    {
+        return [
+            'id' => 'notification-id',
+            'user_id' => 'user-id',
+            'title' => $title,
+            'message' => 'Message',
+            'type' => 'info',
+            'channel' => 'web',
+            'priority' => 'normal',
+            'state' => 'no_leido',
+            'read' => false,
+            'icon_id' => '',
+            'created_at' => '2026-07-12T00:00:00Z',
+            'read_at' => '',
+        ];
     }
 }
diff --git a/tests/Feature/GatewayAuthenticationTest.php b/tests/Feature/GatewayAuthenticationTest.php
index 6d562c5..b4e72fd 100644
--- a/tests/Feature/GatewayAuthenticationTest.php
+++ b/tests/Feature/GatewayAuthenticationTest.php
@@ -2,19 +2,76 @@
 
 namespace Tests\Feature;
 
+use App\Contracts\GatewayClient;
+use App\Exceptions\GatewayRpcException;
+use App\Models\User;
 use Illuminate\Foundation\Testing\RefreshDatabase;
-use Illuminate\Foundation\Testing\WithFaker;
+use Mockery\MockInterface;
 use Tests\TestCase;
 
 class GatewayAuthenticationTest extends TestCase
 {
-    /**
-     * A basic feature test example.
-     */
-    public function test_example(): void
+    use RefreshDatabase;
+
+    public function test_local_authentication_without_a_gateway_session_is_rejected(): void
+    {
+        $user = User::factory()->create();
+
+        $response = $this->actingAs($user)->get(route('dashboard'));
+
+        $response
+            ->assertRedirect(route('login'))
+            ->assertSessionHasErrors('email');
+        $this->assertGuest();
+    }
+
+    public function test_a_temporary_notification_failure_does_not_destroy_the_session(): void
     {
-        $response = $this->get('/');
+        $user = User::factory()->create();
+        $this->mock(GatewayClient::class, function (MockInterface $mock): void {
+            $mock->shouldReceive('countUnread')
+                ->once()
+                ->andThrow(new GatewayRpcException(
+                    'notificaciones.v1.NotificationService/CountUnread',
+                    14,
+                    'Unavailable.',
+                ));
+        });
+
+        $response = $this->actingAs($user)
+            ->withSession([
+                'gateway.access_token' => 'access-token',
+                'gateway.user_id' => 'user-id',
+            ])
+            ->get(route('dashboard'));
+
+        $response
+            ->assertOk()
+            ->assertSee('Notifications are temporarily unavailable.');
+        $this->assertAuthenticatedAs($user);
+    }
+
+    public function test_remote_logout_failure_does_not_block_local_logout(): void
+    {
+        $user = User::factory()->create();
+        $this->mock(GatewayClient::class, function (MockInterface $mock): void {
+            $mock->shouldReceive('logout')
+                ->once()
+                ->andThrow(new GatewayRpcException(
+                    'auth.v1.AuthService/Logout',
+                    14,
+                    'Unavailable.',
+                ));
+        });
+
+        $response = $this->actingAs($user)
+            ->withSession([
+                'gateway.access_token' => 'access-token',
+                'gateway.refresh_token' => 'refresh-token',
+            ])
+            ->post(route('logout'));
 
-        $response->assertStatus(200);
+        $response->assertRedirect(route('home'));
+        $this->assertGuest();
     }
 }
diff --git a/phpstan.neon b/phpstan.neon
index 1ca23a4..fca64e8 100644
--- a/phpstan.neon
+++ b/phpstan.neon
@@ -10,4 +10,10 @@ parameters:
         - database/
         - routes/
 
+    excludePaths:
+        - app/Grpc/*
+
+    bootstrapFiles:
+        - phpstan-bootstrap.php
+
     level: 7
diff --git a/pint.json b/pint.json
index 93061b6..2855246 100644
--- a/pint.json
+++ b/pint.json
@@ -1,3 +1,6 @@
 {
-    "preset": "laravel"
+    "preset": "laravel",
+    "exclude": [
+        "app/Grpc"
+    ]
 }
diff --git a/phpstan-bootstrap.php b/phpstan-bootstrap.php
new file mode 100644
index 0000000..247c34c
--- /dev/null
+++ b/phpstan-bootstrap.php
@@ -0,0 +1,7 @@
+<?php
+
+use Google\Protobuf\RepeatedField;
+
+// Loading the public class registers google/protobuf's backward-compatible
+// Google\Protobuf\Internal\RepeatedField alias used by protoc 3.x output.
+class_exists(RepeatedField::class);
